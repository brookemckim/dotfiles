#!/bin/sh

if  [ -z $1 ];then
  echo "Please provide a server to key."
  exit 1
fi

server=$1
key="$HOME/.ssh/id_rsa.pub"

scp $key $server:~/bmckim.pub && ssh $server 'mkdir -p ~/.ssh && cat ~/bmckim.pub >> ~/.ssh/authorized_keys; rm -rf ~/bmckim.pub'
